@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Waypoint {{ $waypoint->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/waypoints') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/waypoints/' . $waypoint->id . '/edit') }}" title="Edit Waypoint"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('waypoints' . '/' . $waypoint->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Waypoint" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $waypoint->id }}</td>
                                    </tr>
                                    <tr><th> Fecha </th><td> {{ $waypoint->fecha }} </td></tr><tr><th> Hora </th><td> {{ $waypoint->hora }} </td></tr><tr><th> DispositivoA Id </th><td> {{ $waypoint->dispositivoA_id }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
